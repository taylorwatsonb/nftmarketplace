import { TrendingUpIcon, FireIcon, ImageIcon } from "lucide-react";
import { LucideIcon } from "lucide-react";

export const iconMap: Record<string, LucideIcon> = {
  "trending-up": TrendingUpIcon,
  "fire": FireIcon,
  "image": ImageIcon,
};